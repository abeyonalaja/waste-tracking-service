{
  "name": "waste-tracking-gateway",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/waste-tracking-gateway/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/waste-tracking-gateway",
        "main": "apps/waste-tracking-gateway/src/main.ts",
        "tsConfig": "apps/waste-tracking-gateway/tsconfig.app.json",
        "assets": ["apps/waste-tracking-gateway/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/waste-tracking-gateway/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "waste-tracking-gateway:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "waste-tracking-gateway:build:development"
        },
        "production": {
          "buildTarget": "waste-tracking-gateway:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/waste-tracking-gateway/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/waste-tracking-gateway/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": []
}
